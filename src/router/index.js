import { createRouter, createWebHistory } from "vue-router";
// import component name from 'path to component'

const routes = [
  // { path: '/', component: compenent name }
];
const router = createRouter({
  history: createWebHistory(),
  routes,
});

export default router;
